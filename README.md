# Agentic RAG Example 

An end to end RAG solution with an evaluation framework. 

### Future work

1. Implement cross encoder for reranking or evaluation.
2. See if llm client can be abstracted such that any provider can be used.
3. implement more chucking methods.
4. use words in evaluation instead of 1/0.
5. maybe consider expanding reason in evaluation? might not require if we can get business input. 
